node_modules/
lib/
*.log
*.bak-*
scripts/inspect-db.mjs
# feishu-relay 的真实配置含群机器人 webhook 凭据（token 在 URL 路径里），只入库 example
scripts/feishu-relay.config.json
# lockfile 由 npm 从 npx 缓存的 @deepseek-ai junction 解析出绝对路径
# （含本机用户名/缓存路径），不入库（npm pack 也会自动排除它）
package-lock.json

shots/
*.tgz
scripts/Delete_*
